# MCFunction editor.

tests:
    /options/minecraft: true

priority: 105
data:
    editors:
        # Extensionless files in the functions directory are mcfunction files
      - regex: ^functions/(.*/)?[^\.]*$
        type: mcfunction

        # files ending in .mcfunction are mcfunction files
      - regex: \.mcfunction$
        type: mcfunction
